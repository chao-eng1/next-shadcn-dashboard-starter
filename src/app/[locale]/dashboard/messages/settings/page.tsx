'use client';

import React from 'react';
import { MessageSettings } from '@/components/messages/message-settings';

export default function MessageSettingsPage() {
  return (
    <div className="h-full">
      <MessageSettings />
    </div>
  );
}